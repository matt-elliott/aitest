{
  "name": "log-viewer",
  "version": "1.0.0",
  "description": "Web-based log viewer for Xubuntu server logs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-service": "sudo cp log-viewer.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable log-viewer",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": ["logs", "viewer", "monitoring", "server"],
  "author": "System Administrator",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2",
    "chokidar": "^3.5.3",
    "moment": "^2.29.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}